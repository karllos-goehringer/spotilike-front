name: spotilike_front
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.2.3
  audio_video_progress_bar: ^2.0.3
  just_audio: ^0.10.5
  just_audio_background: ^0.0.1-beta.17
  rxdart: ^0.28.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

assets:
   - assets/music/
   - assets/images/
   